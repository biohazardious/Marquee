#
# Runtime dependencies. pyproject.toml is the source of truth; this file is kept for
# `pip install -r`.
#
# NOTE: the `configparser` backport used to be pinned here. On Python 3 it shadows
# the standard library module of the same name, so it was dropped. tqdm went the same
# way when progress moved behind the Reporter protocol.
#
pyasn1==0.4.8
    # via pysmb
pysmb==1.2.9.1
    # via -r requirements.in
