{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.marquee_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.healthcheck.set_test("http", {"port": values.network.web_port.port_number, "path": "/api/health"}) %}
{% do c1.environment.add_env("MARQUEE_PORT", values.network.web_port.port_number) %}
{% if values.marquee.api_key %}
  {% do c1.environment.add_env("MARQUEE_API_KEY", values.marquee.api_key) %}
{% endif %}
{% do c1.environment.add_user_envs(values.marquee.additional_envs) %}

{% do c1.add_port(values.network.web_port) %}

{% do c1.add_storage("/config", values.storage.config) %}
{% do perm_container.add_or_skip_action("config", values.storage.config, perms_config) %}

{#- The torrent folder is the download client's as much as Marquee's: its ownership is never changed here. -#}
{% do c1.add_storage(values.storage.downloads.mount_path, values.storage.downloads) %}

{% do c1.add_storage(values.storage.library.mount_path, values.storage.library) %}
{% do perm_container.add_or_skip_action("library", values.storage.library, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}
{% do tpl.notes.set_body("Unless an API key was set, one is generated on the first start and printed in the logs with the URL to open.") %}

{{ tpl.render() | tojson }}
